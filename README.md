# Keycloak-demo-journey
Keycloak is an open-source IAM solution that secures Java web apps via authentication and authorization. It enables single sign-on (SSO) and issues tokens (OAuth2/OpenID Connect) to verify users. Java apps use Keycloak adapters to validate tokens and enforce RBAC. This centralizes user and session management, simplifies security.
